name: Game Engine
on: [workflow_dispatch]
jobs:
  run-step:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Execute Step
        env:
          API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          # Здесь будет вызов вашего скрипта, который взаимодействует с Gemini
          echo "Запуск шага разработки..."
